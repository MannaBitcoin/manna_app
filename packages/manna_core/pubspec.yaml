name: manna_core
description: "Manna's internal package encapsulating LWK and Boltz-client along with util rust code"
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.10.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_rust_bridge: 2.12.0
  hooks: ^2.0.2
  native_toolchain_rust: ^1.0.4+0

  collection: any
dev_dependencies:
  flutter_lints: ^6.0.0
